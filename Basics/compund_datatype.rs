fn main(){
    let numbers:[i32; 5] = [1, 2, 3, 4, 5];
    println!("array of numbers are {:?}", numbers);
}
//need to use "" for print not ''
//without :? inside {} (println!) error is "cannot format with the default formater"
