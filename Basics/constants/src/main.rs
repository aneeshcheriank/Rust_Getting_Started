//Constants
fn main() {
    const X: u8 = 10;
    println!("Value of the constant is {}", X);
    println!("Value of 3 Hours in second is {}", THREE_HOURS_IN_SECOND);
}

const THREE_HOURS_IN_SECOND: u16 = 3*60*60;
